- example programs ???
- add checks=2
- add write() methods so we can change data and write out a new dump file
- V2 dump format
- should print methods return printable data instead of printing data directly?
- decode/encode tl_data, key_data, e_data ???
- better format (objects?) for tl_data, key_data, e_data ???
